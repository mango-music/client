import React from 'react';
import RatingListEntry from './RatingListEntry';

const RatingList = () => {
  return (
    <ul>
      <RatingListEntry />
      <RatingListEntry />
      <RatingListEntry />
    </ul>
  );
};

export default RatingList;
